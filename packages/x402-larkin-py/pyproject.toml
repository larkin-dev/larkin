[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "larkin-x402"
version = "1.0.0"
description = "Python SDK for Larkin — wallet authorization for x402-paid APIs."
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
keywords = ["x402", "larkin", "agent", "wallet", "authorization"]
authors = [{ name = "Michael Bomhoff" }]
classifiers = [
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
]
dependencies = [
  "httpx>=0.27",
  "pynacl>=1.5",
]

[project.optional-dependencies]
dev = ["pytest>=7"]

[project.urls]
Homepage = "https://larkin.sh"
Repository = "https://github.com/larkin-dev/larkin-public-tmp.git"
"Bug Tracker" = "https://github.com/larkin-dev/larkin-public-tmp/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/larkin_x402"]
