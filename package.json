{
  "name": "larkin",
  "private": true,
  "version": "0.1.0",
  "description": "Authorization middleware for the x402 agent economy.",
  "workspaces": [
    "packages/*",
    "tests/x402-demo"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "devDependencies": {
    "@noble/ed25519": "^2.1.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
