# Changelog

This file records notable changes across all published Larkin packages. Each package also maintains its own `CHANGELOG.md` under `packages/<name>/`.

The packages follow [Semantic Versioning](https://semver.org/) and this file follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

## Unreleased

Initial public release of all packages will land here when the corresponding npm or PyPI publish happens. Tracked packages:

- `@larkinsh/x402` (npm)
- `@larkinsh/verify` (npm)
- `@larkinsh/mcp` (npm)
- `larkin-x402` (PyPI)
